import 'package:flutter/material.dart';

class NavigatorControls extends StatelessWidget {
  const NavigatorControls({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
